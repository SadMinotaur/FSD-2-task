include ../../sharedElements/inputbox/inputbox

style
  include login.scss

mixin login()
  -
      var log = "login"
  .login
    .border
      .loginText Войти
      +inputBox("Email")
      +inputBox("Пароль")
